<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <!-- Primary theme color of the app (sets background color of app bar) -->
        <item name="colorPrimary">@color/accent_material_light_2</item>
        <!-- Background color of buttons in the app -->
        <item name="colorButtonNormal">#80CBC4</item>
    </style>

    <style name="newTreme" parent="Theme.AppCompat.Light.NoActionBar" >
        <item name="colorAccent">@color/accent_material_light_2</item>
    </style>
</resources>
